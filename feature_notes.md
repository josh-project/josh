

- RAM cache (LRU) instead of sled
    With notes as fallback that could be enough

- PIN to sha: potential obsolete with "josh link"

- perf: move to gitoxide, packfiles, in memory ops...

- api for filter building, filter as tree all the way

- distributed cache is not same repo, sharded, sparse

- repo sharding
