[project]
name = "bench"
version = "0.1.0"
description = "Benchmarks for josh-filter against other history-rewriting tools"
requires-python = ">=3.14"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["bench"]
