query GetRepositoryCollaborators($owner: String!, $name: String!, $first: Int!, $after: String) {
  repository(owner: $owner, name: $name) {
    collaborators(first: $first, after: $after) {
      pageInfo {
        endCursor
        hasNextPage
      }
      edges {
        ..._CollaboratorEdgeInfo
      }
    }
  }
}
